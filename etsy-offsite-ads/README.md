# Etsy Off-Site Ads (Printify)

Toggle **Etsy off-site ads** in Printify for single products, bulk sync, desktop GUI, or **automatic watch mode**.

## GitHub (set-and-forget)

See **[`../GITHUB_SETUP.md`](../GITHUB_SETUP.md)** at the repo root.

## Full documentation

**[`RUN.md`](RUN.md)** — install, login, CLI, bulk sync, desktop, watch mode, troubleshooting.

## Quick commands

```powershell
npm install
npx playwright install chromium
copy .env.example .env

npm run login
npm run watch
```
